// This is the application entry. We'll setup our server here
const http = require('http');
const app = require('../app');

const port = 8081;
app.set('port', port);

const server = http.createServer(app);

server.listen(port);